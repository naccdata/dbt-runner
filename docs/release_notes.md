# Release Notes

## 0.2.0 [2025-09-25]

__Enhancements__:
*For new features, improvements, or changes in behavior that add functionality.*

- Example: Add `max-array-size` config option.
- Example: Improve handling of multi-frame DICOM files.

__Fixes__:
*For bug fixes that correct incorrect behavior or prevent errors.*

- Example: Fix crash when SOPClassUID is missing.
- Example: Correct regex for parsing private tags.

__Maintenance__:
*For internal or non-feature changes such as dependency bumps,
CI/CD updates, Dockerfile adjustments, or security patches.*

- Example: Upgrade to `flywheel/python:3.12-alpine`.
- Example: Resolve HIGH and CRITICAL vulnerabilities.

__Documentation__:
*For updates to README, user guides, mkdocs, or code docstrings.*

- Example: Update configuration options in README.
- Example: Add usage examples to mkdocs site.

__Breaking Changes__:
*For changes that are __not backward compatible__
and require users to update
their workflow, configs, or code.*

- Example: Change default for `max_geometric_splits` to `-1` (skip by default).
- Example: Rename gear to `dicom-splitter`.

---

## 0.1.1 [2025-09-10]

__Fixes__:

- Example bug fix entry.

__Enhancements__:

- Example enhancement entry.

---

## 0.1.0 [2025-09-01]

*Initial development release.*
